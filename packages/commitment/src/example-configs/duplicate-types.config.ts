import { Config } from "commitment";

const config: Config = {
    types: {
        override: [
            {
                name: "Feat",
                value: "feat",
            },

            {
                name: "Feature",
                value: "feat",
            },
        ],
    },
};

export default config;
